# Claude Code — configurações locais/pessoais (podem conter permissões e dados da máquina)
.claude/settings.local.json
.claude/*.local.*

# Node / build
node_modules/
dist/
build/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# Foundry VTT — pacotes gerados e locks do LevelDB dos compêndios
*.zip
packs/
packs/**/LOCK
packs/**/LOG
packs/**/LOG.old
foundry.config.*
.foundry/

# graphify — grafo gerado (recriar com `graphify update .`)
graphify-out/

# Ambiente e segredos
.env
.env.*
!.env.example

# SO / editores
.DS_Store
Thumbs.db
desktop.ini
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# Cobertura de testes
coverage/
